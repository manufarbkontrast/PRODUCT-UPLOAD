# Anwendung
NODE_ENV=development

# ┌─────────────────────────────────────────────────────┐
# │ SITE URL – Hier die Domain eintragen!               │
# │                                                     │
# │ Lokal:      http://localhost:3000                   │
# │ Produktion: https://deine-domain.de                 │
# └─────────────────────────────────────────────────────┘
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Compose-Profile: "dev" lokal, "prod" auf Servern
COMPOSE_PROFILES=dev
# Produktion: Domain fuer Caddy SSL-Zertifikat (z.B. spz.deine-domain.de)
SITE_DOMAIN=localhost
ACME_EMAIL=admin@example.com
# Optional: Subdomain fuer n8n (z.B. n8n.deine-domain.de)
N8N_DOMAIN=

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Postgres (lokal für pgAdmin, n8n etc.)
POSTGRES_USER=postgres
POSTGRES_DB=app
POSTGRES_PASSWORD=change-me

# Auth (App-Login: Benutzername + 4-Ziffern PIN)
APP_USERNAME=admin
APP_PIN=1234
ADMIN_TOKEN=please-change-me
# Für lokale Entwicklung kann AUTH_DISABLED=true gesetzt werden
AUTH_DISABLED=false

# n8n (Basic Auth aktiv, auch wenn nur lokal erreichbar)
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change-me

# n8n E-Mail (lokal via Mailpit)
N8N_EMAIL_MODE=smtp
N8N_SMTP_HOST=mailpit
N8N_SMTP_PORT=1025
N8N_SMTP_SSL=false
N8N_SMTP_USER=
N8N_SMTP_PASS=
N8N_SMTP_SENDER="n8n <no-reply@localhost>"
N8N_PROTOCOL=http
# Lokal: Host erzeugt Links in E-Mails; für Server Domain eintragen
N8N_HOST=localhost:5678
# Optional: Setzen, wenn n8n hinter einem Proxy/Domain erreichbar ist
N8N_WEBHOOK_URL=

# pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=change-me

# Google OAuth2
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/google/callback

# Google Drive/Sheets
GOOGLE_DRIVE_FOLDER_ID=
GOOGLE_SHEET_ID=
GOOGLE_SHEET_NAME=Tabellenblatt1

# Gemini API (Bildbearbeitung via Gemini Flash)
# Get your API key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Remove.bg API (optional, wird in n8n verwendet)
# Get your free API key at: https://www.remove.bg/api
# Free tier: 50 Bilder/Monat
REMOVEBG_API_KEY=
