# Produkt-Erfassung — Integration SDD-Ledger
Plan: obsidian/30_Projekte/KI_Automatisierung/Produkt_Erfassung_Integration_Plan.md
Repo: ~/Downloads/product-upload (github manufarbkontrast/PRODUCT-UPLOAD)
Ziel-DB: mycrafton (genzhiywvfrsouhnkise), gemeinsam mit Brain Cockpit.

Phase 1 DB-Fundament: DONE (Migration 20260715 in mycrafton applied — products+product_images (RLS an, keine anon-Policies), Buckets product-images/processed-images. profiles hat bereits role+filiale (PK=id). jtl_articles/jtl_stock_locations existieren (Cockpit-Sync); jtl_articles.name != Upload-Erwartung artikel_name -> im jtl-lookup-Code mappen.)

Task P1 App→mycrafton: complete (require-filiale auf profiles.id + Rollen-Gate, /api/jtl-lookup kanonisch, EanScanner auf jtl-lookup, Google-Drive-JSON- + Gemini-Vision-Pfade entfernt, review clean).
Task P3 Barcode-Scan mobil härten: complete (commits b1484d5..85dea3b + fix 139efd0, re-review Approved — scan-crop-Geometrie via lib/scan-crop.ts an object-cover-Sichtfenster angeglichen, 4 Geometrie-Tests, Suite 102/102 grün).
Task P4a Foto-Ansichten + Grafiken: complete (commit d780ab6, review Approved — 4 kanonische Views seite_aussen/sohle/schraeg_vorne/paar_profil in config/shoe-views.ts mit anweisung/piktogramm/silhouette, 8 SVGs in public/foto-guide/, gemini-classifier + image-processing + ShoeViewIndicator konsistent auf 4 umgestellt, 15 Tests). Minor-Follow-up: scripts/audit-drive-images.ts (Standalone, nicht importiert) nutzt noch alte 5 Keys.
Task P4b Geführte Foto-Aufnahme: complete (commit 4d4bd97 + fix a251f0f, re-review Approved — components/GuidedPhotoCapture.tsx (3-Phasen-Flow Anleitung/Aufnahme/Vorschau, Live-Silhouetten-Overlay pointer-events-none opacity-40, environment-Kamera), lib/guided-capture.ts (immutabler State-Machine, 18 Tests) + lib/camera-errors.ts (extrahiert, EanScanner unangetastet) + lib/sort-order.ts (parseSortOrder bounded [0,SHOE_VIEWS.length), 11 Tests). Upload via bestehendem POST /api/products/{id}/images (additiver sortOrder-Param, 400 bei ungültig), danach POST /process. Klassischer ImageUploader-Fallback bleibt. Kamera-Stream-Cleanup-Guard gegen Leak bei Abbruch während Permission-Prompt. Suite 131/131 grün.
Task P5 Deploy-Vorbereitung (in-repo, Task 8): complete (commits e375943 + 128173e + doc-fixes, review Approved nach 2 Doc-Fixes — basePath /erfassung in next.config.ts, lib/base-path.ts withBasePath (7 Tests) an ALLE rohen fetch('/api...') + /public-Asset-src (foto-guide SVGs) angewandt (Sweep: 0 vergessen, 0 doppelt), Drive optional via isDriveConfigured() in lib/google/auth.ts (Status 'uploaded' statt 'drive_error' wenn Drive-Env fehlt), deploy/{Dockerfile,docker-compose.yml,Caddyfile} + .github/workflows/deploy.yml + .env.example. Caddy: 'handle' NICHT 'handle_path' (sonst 404). Suite 142/142 grün, standalone-Build ok. Empirisch gefundene Deploy-Bugs vorab gefixt: handle_path-Prefix-Strip, turbopack.root workspace-misdetection.)

Task P5 Live-Deploy (Task 8+9): LIVE (2026-07-15). App erreichbar unter https://167.233.218.238/erfassung, Cockpit weiter auf / . Secrets in /opt/erfassung/deploy/.env (chmod 600, GEMINI + service_role + anon + basePath). GitHub-Secrets HETZNER_HOST+HETZNER_SSH_KEY gesetzt. CI (.github/workflows/deploy.yml) baut auf dem Server (ssh docker compose build+up).
  Verifiziert live: /erfassung/login 200, /erfassung/api/health {"status":"ok"}, geschützte Routen -> /erfassung/login (einfacher Prefix), Assets + foto-guide-SVGs unter /erfassung/_next bzw. /erfassung/foto-guide (200), Cockpit-Root unberührt.
  Deploy-Bugs unterwegs gefixt: (a) Lint-Gate rot (vorbestehende Fehler) -> eslint _archive ignorieren + any in Tests erlaubt + AuthContext ref->useState + ReorderButton setState-microtask; (b) next build OOM auf 2GB-Host -> NODE_OPTIONS=--max-old-space-size=3072 + 2. Swapfile (jetzt 7GB swap); (c) Caddy stale-single-file-bind-mount -> reload lud alte Config, Fix = docker restart deploy-caddy-1; (d) Caddy-Matcher: `handle /erfassung/*` matcht nacktes /erfassung nicht -> `@erf path /erfassung /erfassung/*`; (e) Fehldiagnose Middleware-Redirect: Next ergänzt basePath bei NextResponse.redirect SELBST -> reiner Pfad '/login' korrekt (withBasePath verdoppelte).
  FRAGIL / offen: Die /erfassung-Route lebt in /opt/brain-cockpit/deploy/Caddyfile (Cockpit-Repo, NICHT im Upload-Repo) -> bei Cockpit-Redeploy, der die Caddyfile überschreibt, geht die Route verloren; Muster dokumentiert in deploy/Caddyfile. Compose-Projektnamen-Kollision (beide "deploy") -> `docker compose down` in einem Ordner trifft beide Container.
  NOCH ZU TUN (User, am Gerät): echter End-to-End-Test — verkaeufer-Login, Barcode-Scan am iPhone, geführter Foto-Flow (4 Schritte + Overlay), Fotos in Supabase Storage, Gemini-Verarbeitung.
P2 Rollen-Weiche im Cockpit: PENDING — jetzt möglich (Tool ist live), verkaeufer -> /erfassung, admin/buero -> Cockpit + Link.

## myCrafton-Umbau (2026-07-16, Plan: obsidian myCrafton_Umbau_Plan.md)
Phase D Datenfrische: complete+live — pg_cron timebutler-hours-2h (10 5-19/2 GMT, API-Limit 12/Tag, 4 Reserve), sync-watchdog Edge Function + 30min-Cron (slot-genau, Dedup sync_alert_state, Slack via Vault slack_webhook_url, E2E getestet), Cockpit: sync-freshness-Ampel (0c80779+6810c9b, NULLS-LAST-Kontrakt gepinnt), Refresh-Button status-bewusst mit In-Flight-Guard (53ee113+517b750, re-review Approved), revalidate 300->60.
Phase A UI-Angleich: complete+live — Design-System-Port (369f5d0, review Approved, shadcn/-Unterordner wg. Case-Kollision), Restyle 3 Commits e950b61/0d83feb/246d1ea + fix 15e332b (Tap-Targets 44px, -600-Farben -> dark-Konvention; review-Findings behoben). Erfassung permanent dark wie Cockpit (html class="dark"), zinc=0, Legacy Button/Input geloescht, Select/Textarea restyled.
Phase B Accounts: verifiziert — ein Auth-Store (mycrafton), SSO via geteiltem sb-*-Cookie, Rollen deckungsgleich. Caveats: Logout wirkt app-uebergreifend (SSO-normal); 2 verkaeufer-Profile ohne filiale -> 403 in Erfassung (USER muss filiale setzen).
Phase C1 Rename sichtbar: complete+live — myCrafton in manifest/title/login/nav, Icons MC regeneriert, package.json name mycrafton, Cloudflare/OpenNext komplett entfernt (4f87153+da76a80+0d941f8, review Approved).
OFFEN: C2 strukturell (GitHub-Repo brain-cockpit->myCrafton + Server-Pfad /opt/brain-cockpit->/opt/mycrafton + deploy.yml-Pfade; Wartungsfenster MIT User). Minor-Follow-up: Refresh-Button "laeuft"-Detail-Text unterscheiden (in-flight vs. laeuft-noch).
Phase C2 Rename strukturell: complete+live (2026-07-16) — GitHub-Repo manufarbkontrast/brain-cockpit -> manufarbkontrast/myCrafton (gh repo rename, Redirects aktiv), lokal ~/Downloads/mycrafton, Server /opt/brain-cockpit -> /opt/mycrafton (stop app+caddy -> mv -> compose up aus neuem Pfad, Ausfall ~Sekunden, erfassung lief durch), deploy.yml+README+Erfassungs-Doku-Pfade nachgezogen, E2E-Deploy ueber neuen Pfad gruen. SSH-Key-Dateiname ~/.ssh/brain-cockpit-hetzner bewusst unveraendert. myCrafton-Umbau damit KOMPLETT.
Umlagerung v2 (Cockpit, 2026-07-16): complete+live — lib/umlagerung.ts neu: umlagerungsEmpfehlungen(items7, bestaende) mit nachfuellen (verkauft 7d + Bestand exakt 0 + Spender>=1) und abgeben (Rest <=3 Stk oder <=2 Groessen -> an Filiale mit >= Verkaeufen, Tiebreak Restbestand/alphabetisch; ersetzt nachfuellen), deutsche Begruendungs-Templates mit Zahlen. Auf filiale/[code] (eigener 7d-All-Filialen-Fetch), /bestand-Seite + Nav/Mobile-Tabs geloescht, Flop-10 auf Marken-Detail raus (topFlop->top vereinfacht). Commits a68571d+ea44b85+fix 30f8d71 (Review-Fixes: gegenseitige-Abgabe-Aufloesung nach Restbestand/alphabetisch, Bestand-Dedup last-write, echtes 7-Tage-Fenster, KEY_SEP-Kollisionstest [Review-Fund war Falsch-Positiv: U+0001 ist im Diff unsichtbar]). 181/181 Tests.
  Nebenwirkung (User-Entscheidung offen): "Ausverkaufte Topseller" + "Ladenhueter"-Karten starben mit /bestand; getAusverkaufteTopseller/getLadenhueter/getJungeSkus jetzt tote Exports in lib/queries.ts (wiederherstellbar aus git).
  Folge-Task #73 (nach Praxis-Feedback): Gemini-Umlagerungs-Agent Morgen-Digest nach Slack.
